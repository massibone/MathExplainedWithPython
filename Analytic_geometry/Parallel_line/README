Parellal line

Two lines that run parallel to each other are always the same distance apart and have equal gradients.

If two lines intersect perpendicularly, then the product of their gradients is equal to 
−
1
.
